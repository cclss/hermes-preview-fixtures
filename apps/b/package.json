{
  "name": "monorepo-app-b",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "mkdir -p dist && printf '<!doctype html><h1>PREVIEW E2E STATIC app-b</h1>' > dist/index.html"
  },
  "devDependencies": { "vite": "^5.0.0" }
}
